export interface EmployeeContract {
    id: number;
    start_date: string; // You can use Date type if you want to handle Date objects
    end_date: string; // Same as above
    position: string;
    employee_id: number;
}
